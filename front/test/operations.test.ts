import { describe, expect, test } from '@jest/globals';

describe('operation module', () => {
    
    test('add new operation locally', () => {
        expect(1 + 2).toBe(3);
    });

    test('update operation locally', () => {
        expect(1 + 2).toBe(3);
    });

    test('delete operation locally', () => {
        expect(1 + 2).toBe(3);
    });

    test('update daughter operation locally', () => {
        expect(1 + 2).toBe(3);
    });

    test('delete daughter operation locally', () => {
        expect(1 + 2).toBe(3);
    });
});